---
phase: 02-filter-panel-ui
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/app/prototype/clean-map/components/FilterPanel.tsx
  - frontend/app/prototype/clean-map/components/SearchBar.tsx
  - frontend/app/prototype/clean-map/components/LocationSelector.tsx
  - frontend/app/prototype/clean-map/components/ChipGroup.tsx
  - frontend/app/prototype/clean-map/components/RoomSection.tsx
  - frontend/app/prototype/clean-map/components/BillingSection.tsx
  - frontend/app/prototype/clean-map/components/index.ts
  - frontend/app/prototype/clean-map/page.tsx
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, FILT-06, FILT-07, FILT-08, UX-03]

must_haves:
  truths:
    - "User sees a text search input at the top of the filter panel with placeholder 'Cari nama atau alamat kos...'"
    - "User sees a searchable campus building selector with placeholder 'Pilih gedung kampus...'"
    - "User sees accordion section 'Kamar' containing gender chips (Putra/Putri/Campuran) and AC chips (AC/Non-AC/Keduanya)"
    - "User sees accordion section 'Pembayaran' containing price range inputs, period selector, and payment type chips"
    - "Gender chips behave as radio (only one active at a time)"
    - "AC chips behave as radio (only one active at a time)"
    - "Payment type chips behave as multi-select (one or more can be active)"
    - "Price min/max inputs accept numeric input"
    - "Period dropdown updates displayed selection"
    - "Campus combobox filters dropdown list as user types"
    - "Search input updates displayed value (no debounce, no filtering)"
    - "Accordion sections expand/collapse on click with chevron rotation"
    - "FilterPanel injected as children of Sidebar in page.tsx, receiving filter state and campus list as props"
    - "All copy in Bahasa Indonesia per UI-SPEC §Copywriting Contract"
  artifacts:
    - path: "frontend/app/prototype/clean-map/components/FilterPanel.tsx"
      provides: "Main filter panel with Accordion shell + standalone SearchBar and LocationSelector bars"
      min_lines: 80
      exports: ["FilterPanel"]
    - path: "frontend/app/prototype/clean-map/components/SearchBar.tsx"
      provides: "Standalone text search input (Cari nama atau alamat kos...)"
      min_lines: 30
      exports: ["SearchBar"]
    - path: "frontend/app/prototype/clean-map/components/LocationSelector.tsx"
      provides: "Searchable campus combobox with Command+Popover"
      min_lines: 60
      exports: ["LocationSelector"]
    - path: "frontend/app/prototype/clean-map/components/ChipGroup.tsx"
      provides: "Reusable chip/toggle group component (radio mode for gender/AC, multi-select mode for payment)"
      min_lines: 40
      exports: ["ChipGroup"]
    - path: "frontend/app/prototype/clean-map/components/RoomSection.tsx"
      provides: "Kamar accordion: gender chips + AC chips"
      min_lines: 30
      exports: ["RoomSection"]
    - path: "frontend/app/prototype/clean-map/components/BillingSection.tsx"
      provides: "Pembayaran accordion: price inputs + period select + payment chips"
      min_lines: 60
      exports: ["BillingSection"]
    - path: "frontend/app/prototype/clean-map/page.tsx"
      provides: "FilterPanel wired into Sidebar children, filter state + campusList passed as props"
      contains: "FilterPanel"
  key_links:
    - from: "frontend/app/prototype/clean-map/page.tsx"
      to: "frontend/app/prototype/clean-map/components/FilterPanel"
      via: "import { FilterPanel } from './components'"
      pattern: "FilterPanel"
    - from: "frontend/components/FilterPanel.tsx"
      to: "frontend/components/ui/accordion"
      via: "import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@/components/ui/accordion'"
    - from: "frontend/components/FilterPanel.tsx"
      to: "frontend/components/ui/input"
      via: "import { Input } from '@/components/ui/input'"
    - from: "frontend/components/LocationSelector.tsx"
      to: "frontend/components/ui/command"
      via: "import { Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem } from '@/components/ui/command'"
---

<objective>
**Create FilterPanel component with standalone search bar, campus location selector, and the Room (Kamar) + Billing (Pembayaran) accordion sections. Wire FilterPanel into page.tsx.**

Purpose: Build the core filter panel UI structure with 2 of 4 accordion sections populated. The standalone search bar and location selector live outside the accordion. Implement visual-interaction filter controls (chips, inputs, selects) per UI-SPEC. All controls are visual-only (no data mutation) — filter state changes at page level but does not affect map markers.

Output:
- FilterPanel.tsx — main component shell with Accordion + standalone bar slots
- SearchBar.tsx — text input with teal focus ring, placeholder "Cari nama atau alamat kos..."
- LocationSelector.tsx — searchable campus combobox using Command+Popover from shadcn
- ChipGroup.tsx — reusable chip/toggle group (radio + multi-select modes)
- RoomSection.tsx — Kamar accordion: gender chip group + AC chip group
- BillingSection.tsx — Pembayaran accordion: price min/max inputs, period Select, payment chip group
- page.tsx wired: FilterPanel replaces previous StatsBar slot in Sidebar children
- index.ts updated: exports FilterPanel + all sub-components
</objective>

<execution_context>
@/home/evan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/evan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-filter-panel-ui/02-CONTEXT.md
@.planning/phases/02-filter-panel-ui/02-UI-SPEC.md
@frontend/app/prototype/clean-map/page.tsx
@frontend/app/prototype/clean-map/components/Sidebar.tsx
@frontend/app/prototype/clean-map/components/index.ts
@frontend/components/ui/accordion.tsx
@frontend/components/ui/input.tsx
@frontend/components/ui/select.tsx
@frontend/components/ui/command.tsx
@frontend/components/ui/popover.tsx
@frontend/components/ui/label.tsx
@frontend/types/kos.ts

**Key decisions encoded in this plan:**
- D-02: 4 accordion sections (this plan: Kamar + Pembayaran; Plan 03: Fasilitas + Peraturan)
- D-03: Search text + Location campus selector as standalone bar above accordion
- D-04: Chip/toggle groups for binary/ternary choices (gender, AC, payment type)
- D-07: Min/max number inputs + period dropdown for price range
- D-08: Text input for search (nama/alamat)
- D-09: Searchable Command+Popover for location (campus building from master_uns)
- D-10: Campus building selector only — no geolocation or radius slider
- D-12: FilterPanel lives as child component injected into Sidebar via children API
- D-14: Components in `frontend/app/prototype/clean-map/components/`

**Design contracts (from UI-SPEC):**
- Copy: All Indonesian per §Copywriting Contract
- Colors: Teal accent (#0f766e), slate-200 inactive bg, chip-active bg teal, chip-active text white
- Spacing: shadcn defaults (4px grid)
- Typography: Body 14px/400, Label 13px/400, Heading 16px/600
- Interactions: Visual-only, no data mutation per §Interaction Contract

**Integration points:**
- FilterPanel receives `filterState` (FilterState), `setFilterState` (Dispatch), and `campusList` (string[]) as props from page.tsx
- page.tsx passes these from its lifted state (set up in Plan 01)
</context>

<interfaces>
FilterState type (defined in page.tsx during Plan 01):
```typescript
type FilterState = {
  searchText: string;
  selectedCampus: string | null;
  selectedGender: string | null;
  selectedAc: string | null;
  priceMin: string;
  priceMax: string;
  pricePeriod: string;
  selectedPaymentTypes: string[];
  selectedFacilities: string[];
  selectedJamMalam: string | null;
  selectedTamuLawanJenis: string | null;
  selectedTamuMenginap: string | null;
  selectedBolehHewan: string | null;
};
```

page.tsx current structure (after Plan 01):
- State: items, destinations, loading, error, sidebarOpen, previewExpanded, filterState, campusList
- Effects: loadKos, loadDestinations, loadCampusList
- Render: MapView + Sidebar( children[ loading/error/empty/preview ] )
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ChipGroup, SearchBar, LocationSelector, and FilterPanel shell components</name>
  <files>
    - frontend/app/prototype/clean-map/components/ChipGroup.tsx
    - frontend/app/prototype/clean-map/components/SearchBar.tsx
    - frontend/app/prototype/clean-map/components/LocationSelector.tsx
    - frontend/app/prototype/clean-map/components/FilterPanel.tsx
    - frontend/app/prototype/clean-map/components/index.ts
  </files>
  <read_first>
    frontend/components/ui/accordion.tsx — exports default Accordion, AccordionItem, AccordionTrigger, AccordionContent
    frontend/components/ui/input.tsx — exports default Input with standard shadcn props
    frontend/components/ui/command.tsx — exports Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem
    frontend/components/ui/popover.tsx — exports Popover, PopoverTrigger, PopoverContent
    frontend/components/ui/select.tsx — exports Select, SelectTrigger, SelectValue, SelectContent, SelectItem
    frontend/components/ui/label.tsx — exports Label
    frontend/app/prototype/clean-map/components/index.ts — current barrel exports
  </read_first>
  <action>
    1. **Create `frontend/app/prototype/clean-map/components/ChipGroup.tsx`:**

       A reusable chip/toggle group component. Supports two modes:
       - **radio** (single select) — for gender, AC, tamu_lawan_jenis
       - **multi** (multi select) — for payment type

       ```tsx
       "use client";

       import type { ReactNode } from "react";

       type ChipOption = {
         value: string;
         label: string;
       };

       type ChipGroupProps = {
         options: ChipOption[];
         value: string | string[] | null;
         onChange: (value: string | string[] | null) => void;
         mode: "radio" | "multi";
         label?: string;
       };

       export function ChipGroup({ options, value, onChange, mode, label }: ChipGroupProps) {
         const isSelected = (optValue: string) => {
           if (mode === "radio") return value === optValue;
           return Array.isArray(value) && value.includes(optValue);
         };

         const handleClick = (optValue: string) => {
           if (mode === "radio") {
             onChange(value === optValue ? null : optValue);
           } else {
             const current = Array.isArray(value) ? value : [];
             onChange(
               current.includes(optValue)
                 ? current.filter((v) => v !== optValue)
                 : [...current, optValue]
             );
           }
         };

         return (
           <div>
             {label && <p className="text-sm font-medium text-muted-foreground mb-2">{label}</p>}
             <div className="flex flex-wrap gap-2">
               {options.map((opt) => (
                 <button
                   key={opt.value}
                   type="button"
                   onClick={() => handleClick(opt.value)}
                   className={`
                     inline-flex items-center justify-center rounded-md px-3 py-1.5 text-sm font-medium
                     transition-colors duration-150 cursor-pointer
                     ${
                       isSelected(opt.value)
                         ? "bg-[#0f766e] text-white shadow-sm"
                         : "bg-slate-200 text-slate-700 hover:bg-slate-300"
                     }
                   `}
                   aria-pressed={isSelected(opt.value)}
                 >
                   {opt.label}
                 </button>
               ))}
             </div>
           </div>
         );
       }
       ```

    2. **Create `frontend/app/prototype/clean-map/components/SearchBar.tsx`:**

       Text search input for nama/alamat. Full width, prominent position. Per UI-SPEC §Primary Visual Anchor — the search bar is the first element in the filter panel.

       ```tsx
       "use client";

       import { Input } from "@/components/ui/input";

       type SearchBarProps = {
         value: string;
         onChange: (value: string) => void;
       };

       export function SearchBar({ value, onChange }: SearchBarProps) {
         return (
           <div className="px-4 pt-3 pb-2">
             <Input
               type="text"
               placeholder="Cari nama atau alamat kos..."
               value={value}
               onChange={(e) => onChange(e.target.value)}
               className="w-full focus-visible:ring-2 focus-visible:ring-[#0f766e]"
             />
           </div>
         );
       }
       ```

    3. **Create `frontend/app/prototype/clean-map/components/LocationSelector.tsx`:**

       Searchable campus combobox using shadcn Command + Popover. Accepts `campusList` as prop (fetched from page level in Plan 01). Per D-09 and D-10: campus building selector only, no geolocation.

       ```tsx
       "use client";

       import { useState } from "react";
       import { Button } from "@/components/ui/button"; // shadcn button (npx shadcn@latest add button if not installed)
       import { Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem } from "@/components/ui/command";
       import { Popover, PopoverTrigger, PopoverContent } from "@/components/ui/popover";
       import { ChevronDown } from "lucide-react";

       // Note: If `button` shadcn component is not installed, run:
       //   cd frontend && npx shadcn@latest add button
       // Alternatively, use a simple <button> with Tailwind classes instead of shadcn Button.

       type LocationSelectorProps = {
         campusList: string[];
         value: string | null;
         onChange: (value: string | null) => void;
       };

       export function LocationSelector({ campusList, value, onChange }: LocationSelectorProps) {
         const [open, setOpen] = useState(false);

         return (
           <div className="px-4 pb-2">
             <p className="text-sm font-medium text-muted-foreground mb-1.5">Lokasi</p>
             <Popover open={open} onOpenChange={setOpen}>
               <PopoverTrigger asChild>
                 <Button
                   variant="outline"
                   role="combobox"
                   aria-expanded={open}
                   className="w-full justify-between text-sm font-normal"
                 >
                   {value
                     ? campusList.find((c) => c === value) ?? value
                     : "Pilih gedung kampus..."}
                   <ChevronDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
                 </Button>
               </PopoverTrigger>
               <PopoverContent className="w-[--radix-popover-trigger-width] p-0">
                 <Command>
                   <CommandInput placeholder="Cari kampus..." className="h-9" />
                   <CommandList>
                     <CommandEmpty>Gagal memuat data kampus. Coba refresh halaman.</CommandEmpty>
                     <CommandGroup>
                       {campusList.map((campus) => (
                         <CommandItem
                           key={campus}
                           value={campus}
                           onSelect={(currentValue) => {
                             onChange(currentValue === value ? null : currentValue);
                             setOpen(false);
                           }}
                           data-selected={value === campus}
                         >
                           {campus}
                         </CommandItem>
                       ))}
                     </CommandGroup>
                   </CommandList>
                 </Command>
               </PopoverContent>
             </Popover>
           </div>
         );
       }
       ```

       **Important:** If `frontend/components/ui/button.tsx` doesn't exist, install it first:
       ```bash
       cd frontend && npx shadcn@latest add button
       ```
       Then replace the `Button` import path above accordingly.

    4. **Create `frontend/app/prototype/clean-map/components/FilterPanel.tsx` shell:**

       The main filter panel component. Wraps SearchBar + LocationSelector as standalone bar, then an Accordion with Kamar and Pembayaran sections. The shell includes named slots for FacilitiesSection and RulesSection (to be added in Plan 03 via TODO comment). Per D-12, this component injects into Sidebar children.

       ```tsx
       "use client";

       import type { Dispatch, SetStateAction } from "react";
       import {
         Accordion,
         AccordionItem,
         AccordionTrigger,
         AccordionContent,
       } from "@/components/ui/accordion";
       import { SearchBar } from "./SearchBar";
       import { LocationSelector } from "./LocationSelector";
       import { RoomSection } from "./RoomSection";
       import { BillingSection } from "./BillingSection";

       // Plan 03 will import and wire FacilitiesSection and RulesSection here

       type FilterPanelProps = {
         filterState: FilterState;
         setFilterState: Dispatch<SetStateAction<FilterState>>;
         campusList: string[];
       };

       // Re-declare or import FilterState type (it's local to page.tsx — can extract to shared types file)
       type FilterState = {
         searchText: string;
         selectedCampus: string | null;
         selectedGender: string | null;
         selectedAc: string | null;
         priceMin: string;
         priceMax: string;
         pricePeriod: string;
         selectedPaymentTypes: string[];
         selectedFacilities: string[];
         selectedJamMalam: string | null;
         selectedTamuLawanJenis: string | null;
         selectedTamuMenginap: string | null;
         selectedBolehHewan: string | null;
       };

       export function FilterPanel({ filterState, setFilterState, campusList }: FilterPanelProps) {
         const updater = <K extends keyof FilterState>(key: K) => (value: FilterState[K]) => {
           setFilterState((prev) => ({ ...prev, [key]: value }));
         };

         return (
           <div className="flex flex-col">
             {/* ── Standalone Search + Location bar ── */}
             <SearchBar value={filterState.searchText} onChange={updater("searchText")} />
             <LocationSelector
               campusList={campusList}
               value={filterState.selectedCampus}
               onChange={updater("selectedCampus")}
             />

             {/* ── Accordion sections ── */}
             <Accordion type="multiple" className="w-full px-2">
               {/* Kamar */}
               <AccordionItem value="kamar">
                 <AccordionTrigger className="text-base font-semibold px-2">
                   Kamar
                 </AccordionTrigger>
                 <AccordionContent>
                   <RoomSection
                     selectedGender={filterState.selectedGender}
                     selectedAc={filterState.selectedAc}
                     onGenderChange={updater("selectedGender")}
                     onAcChange={updater("selectedAc")}
                   />
                 </AccordionContent>
               </AccordionItem>

               {/* Pembayaran */}
               <AccordionItem value="pembayaran">
                 <AccordionTrigger className="text-base font-semibold px-2">
                   Pembayaran
                 </AccordionTrigger>
                 <AccordionContent>
                   <BillingSection
                     priceMin={filterState.priceMin}
                     priceMax={filterState.priceMax}
                     pricePeriod={filterState.pricePeriod}
                     selectedPaymentTypes={filterState.selectedPaymentTypes}
                     onPriceMinChange={updater("priceMin")}
                     onPriceMaxChange={updater("priceMax")}
                     onPricePeriodChange={updater("pricePeriod")}
                     onPaymentTypesChange={updater("selectedPaymentTypes")}
                   />
                 </AccordionContent>
               </AccordionItem>

               {/* Fasilitas — TODO: Plan 03 */}
               <AccordionItem value="fasilitas">
                 <AccordionTrigger className="text-base font-semibold px-2">
                   Fasilitas
                 </AccordionTrigger>
                 <AccordionContent>
                   {/* FacilitiesSection goes here in Plan 03 */}
                   <p className="text-sm text-muted-foreground px-2 py-4">
                     Fasilitas filter akan ditambahkan.
                   </p>
                 </AccordionContent>
               </AccordionItem>

               {/* Peraturan — TODO: Plan 03 */}
               <AccordionItem value="peraturan">
                 <AccordionTrigger className="text-base font-semibold px-2">
                   Peraturan
                 </AccordionTrigger>
                 <AccordionContent>
                   {/* RulesSection goes here in Plan 03 */}
                   <p className="text-sm text-muted-foreground px-2 py-4">
                     Peraturan filter akan ditambahkan.
                   </p>
                 </AccordionContent>
               </AccordionItem>
             </Accordion>
           </div>
         );
       }
       ```

       **Type extraction note:** The `FilterState` type is duplicated here because it's defined locally in page.tsx. The cleaner approach is to keep it local to FilterPanel.tsx and import it in page.tsx. Adjust page.tsx in Task 3 to import FilterState from FilterPanel rather than defining it inline. **Recommended:** define FilterState type in a shared types file like `frontend/types/filters.ts` in Plan 02, import it in both FilterPanel.tsx and page.tsx.

       **Preferred approach:** Create `frontend/app/prototype/clean-map/components/filter-types.ts` that exports `FilterState` and `DEFAULT_FILTER_STATE`. Import in both FilterPanel.tsx and page.tsx. This avoids type duplication.

    5. **Update `frontend/app/prototype/clean-map/components/index.ts`:**
       Add exports for the new components:
       ```typescript
       // (existing exports remain)
       export * from "./FilterPanel";
       export * from "./SearchBar";
       export * from "./LocationSelector";
       export * from "./ChipGroup";
       export * from "./RoomSection";
       export * from "./BillingSection";
       ```
  </action>
  <verify>
    <automated>
      for f in ChipGroup SearchBar LocationSelector FilterPanel RoomSection BillingSection; do
        test -f "frontend/app/prototype/clean-map/components/${f}.tsx" && echo "${f}: PASS" || echo "${f}: FAIL"
      done
      grep -q "export \* from \"./FilterPanel\"" frontend/app/prototype/clean-map/components/index.ts && echo "INDEX_EXPORTS: PASS" || echo "INDEX_EXPORTS: FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 new component files exist in `frontend/app/prototype/clean-map/components/`
    - `index.ts` exports FilterPanel, SearchBar, LocationSelector, ChipGroup, RoomSection, BillingSection
    - FilterPanel.tsx imports from `@/components/ui/accordion` using the shadcn path alias
    - LocationSelector.tsx imports from `@/components/ui/command` and `@/components/ui/popover`
    - ChipGroup handles both "radio" and "multi" modes
    - SearchBar input has placeholder "Cari nama atau alamat kos..."
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create RoomSection and BillingSection components</name>
  <files>
    - frontend/app/prototype/clean-map/components/RoomSection.tsx
    - frontend/app/prototype/clean-map/components/BillingSection.tsx
  </files>
  <read_first>
    frontend/components/ui/input.tsx — shadcn Input component
    frontend/components/ui/select.tsx — shadcn Select, SelectTrigger, SelectValue, SelectContent, SelectItem
    frontend/components/ui/label.tsx — shadcn Label
    frontend/app/prototype/clean-map/components/ChipGroup.tsx — just created above, knows ChipGroup API
  </read_first>
  <action>
    1. **Create `frontend/app/prototype/clean-map/components/RoomSection.tsx`:**

       Kamar accordion section. Contains:
       - "Jenis Kos" label + ChipGroup (radio mode) with options: Putra, Putri, Campuran
       - "AC" label + ChipGroup (radio mode) with options: AC, Non-AC, Keduanya

       Per UI-SPEC §Copywriting Contract:
       - Gender label: `Jenis Kos`, options: `Putra` / `Putri` / `Campuran`
       - AC label: `AC`, options: `AC` / `Non-AC` / `Keduanya`

       ```tsx
       "use client";

       import { ChipGroup } from "./ChipGroup";

       type RoomSectionProps = {
         selectedGender: string | null;
         selectedAc: string | null;
         onGenderChange: (value: string | null) => void;
         onAcChange: (value: string | null) => void;
       };

       const GENDER_OPTIONS = [
         { value: "Putra", label: "Putra" },
         { value: "Putri", label: "Putri" },
         { value: "Campuran", label: "Campuran" },
       ];

       const AC_OPTIONS = [
         { value: "ac", label: "AC" },
         { value: "non_ac", label: "Non-AC" },
         { value: "keduanya", label: "Keduanya" },
       ];

       export function RoomSection({
         selectedGender,
         selectedAc,
         onGenderChange,
         onAcChange,
       }: RoomSectionProps) {
         return (
           <div className="space-y-4 px-2 py-2">
             <ChipGroup
               label="Jenis Kos"
               options={GENDER_OPTIONS}
               value={selectedGender}
               onChange={onGenderChange}
               mode="radio"
             />
             <ChipGroup
               label="AC"
               options={AC_OPTIONS}
               value={selectedAc}
               onChange={onAcChange}
               mode="radio"
             />
           </div>
         );
       }
       ```

    2. **Create `frontend/app/prototype/clean-map/components/BillingSection.tsx`:**

       Pembayaran accordion section. Contains:
       - "Rentang Harga" label + two Inputs (Min, Maks) side by side
       - "Periode" label + shadcn Select (Bulanan, Semesteran, Tahunan, Per 3 Bulan, Mingguan)
       - "Tipe Pembayaran" label + ChipGroup (multi mode) for payment types

       Per UI-SPEC §Copywriting Contract:
       - Price range label: `Rentang Harga`, min placeholder: `Min`, max placeholder: `Maks`
       - Period label: `Periode`, options: `Bulanan` / `Semesteran` / `Tahunan` / `Per 3 Bulan` / `Mingguan`
       - Payment type label: `Tipe Pembayaran`
       - Price input: type number, no validation (accept any numeric input)

       ```tsx
       "use client";

       import { Input } from "@/components/ui/input";
       import {
         Select,
         SelectTrigger,
         SelectValue,
         SelectContent,
         SelectItem,
       } from "@/components/ui/select";
       import { ChipGroup } from "./ChipGroup";

       type BillingSectionProps = {
         priceMin: string;
         priceMax: string;
         pricePeriod: string;
         selectedPaymentTypes: string[];
         onPriceMinChange: (value: string) => void;
         onPriceMaxChange: (value: string) => void;
         onPricePeriodChange: (value: string) => void;
         onPaymentTypesChange: (value: string | string[] | null) => void;
       };

       const PERIOD_OPTIONS = [
         { value: "mingguan", label: "Mingguan" },
         { value: "bulanan", label: "Bulanan" },
         { value: "per 3 bulan", label: "Per 3 Bulan" },
         { value: "semesteran", label: "Semesteran" },
         { value: "tahunan", label: "Tahunan" },
       ];

       // Payment types — extracted from existing KosClean data patterns
       const PAYMENT_OPTIONS = [
         { value: "bulanan", label: "Bulanan" },
         { value: "semesteran", label: "Semesteran" },
         { value: "tahunan", label: "Tahunan" },
         { value: "per 3 bulan", label: "Per 3 Bulan" },
         { value: "mingguan", label: "Mingguan" },
       ];

       export function BillingSection({
         priceMin,
         priceMax,
         pricePeriod,
         selectedPaymentTypes,
         onPriceMinChange,
         onPriceMaxChange,
         onPricePeriodChange,
         onPaymentTypesChange,
       }: BillingSectionProps) {
         return (
           <div className="space-y-5 px-2 py-2">
             {/* ── Rentang Harga ── */}
             <div>
               <p className="text-sm font-medium text-muted-foreground mb-2">Rentang Harga</p>
               <div className="flex items-center gap-2">
                 <Input
                   type="number"
                   placeholder="Min"
                   value={priceMin}
                   onChange={(e) => onPriceMinChange(e.target.value)}
                   className="flex-1"
                 />
                 <span className="text-muted-foreground">–</span>
                 <Input
                   type="number"
                   placeholder="Maks"
                   value={priceMax}
                   onChange={(e) => onPriceMaxChange(e.target.value)}
                   className="flex-1"
                 />
               </div>
             </div>

             {/* ── Periode ── */}
             <div>
               <p className="text-sm font-medium text-muted-foreground mb-2">Periode</p>
               <Select value={pricePeriod} onValueChange={onPricePeriodChange}>
                 <SelectTrigger className="w-full">
                   <SelectValue placeholder="Pilih periode" />
                 </SelectTrigger>
                 <SelectContent>
                   {PERIOD_OPTIONS.map((opt) => (
                     <SelectItem key={opt.value} value={opt.value}>
                       {opt.label}
                     </SelectItem>
                   ))}
                 </SelectContent>
               </Select>
             </div>

             {/* ── Tipe Pembayaran ── */}
             <ChipGroup
               label="Tipe Pembayaran"
               options={PAYMENT_OPTIONS}
               value={selectedPaymentTypes}
               onChange={onPaymentTypesChange}
               mode="multi"
             />
           </div>
         );
       }
       ```
  </action>
  <verify>
    <automated>
      test -f "frontend/app/prototype/clean-map/components/RoomSection.tsx" && echo "ROOM: PASS" || echo "ROOM: FAIL"
      test -f "frontend/app/prototype/clean-map/components/BillingSection.tsx" && echo "BILLING: PASS" || echo "BILLING: FAIL"
      grep -q "Jenis Kos" frontend/app/prototype/clean-map/components/RoomSection.tsx && echo "JENIS_KOS: PASS" || echo "JENIS_KOS: FAIL"
      grep -q "Rentang Harga" frontend/app/prototype/clean-map/components/BillingSection.tsx && echo "RENTANG: PASS" || echo "RENTANG: FAIL"
      grep -q "Periode" frontend/app/prototype/clean-map/components/BillingSection.tsx && echo "PERIODE: PASS" || echo "PERIODE: FAIL"
      grep -q "Tipe Pembayaran" frontend/app/prototype/clean-map/components/BillingSection.tsx && echo "TIPE_BAYAR: PASS" || echo "TIPE_BAYAR: FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - RoomSection renders gender ChipGroup (radio) with 3 options: Putra, Putri, Campuran
    - RoomSection renders AC ChipGroup (radio) with 3 options: AC, Non-AC, Keduanya
    - BillingSection renders price min/max Inputs with placeholders "Min" / "Maks"
    - BillingSection renders period Select with 5 options (Bulanan, Semesteran, Tahunan, Per 3 Bulan, Mingguan)
    - BillingSection renders payment type ChipGroup (multi) with same period-like options
    - All copy matches UI-SPEC §Copywriting Contract in Bahasa Indonesia
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire FilterPanel into page.tsx with lifted filter state</name>
  <files>
    - frontend/app/prototype/clean-map/page.tsx
  </files>
  <read_first>
    frontend/app/prototype/clean-map/page.tsx — current state after Plan 01 (has FilterState, DEFAULT_FILTER_STATE, filterState, campusList, campus fetch)
    frontend/app/prototype/clean-map/components/FilterPanel.tsx — just created, knows props FilterPanel takes
  </read_first>
  <action>
    1. **Extract FilterState type to a shared file:**
       Create `frontend/app/prototype/clean-map/components/filter-types.ts`:
       ```typescript
       export type FilterState = {
         searchText: string;
         selectedCampus: string | null;
         selectedGender: string | null;
         selectedAc: string | null;
         priceMin: string;
         priceMax: string;
         pricePeriod: string;
         selectedPaymentTypes: string[];
         selectedFacilities: string[];
         selectedJamMalam: string | null;
         selectedTamuLawanJenis: string | null;
         selectedTamuMenginap: string | null;
         selectedBolehHewan: string | null;
       };

       export const DEFAULT_FILTER_STATE: FilterState = {
         searchText: "",
         selectedCampus: null,
         selectedGender: null,
         selectedAc: null,
         priceMin: "",
         priceMax: "",
         pricePeriod: "bulanan",
         selectedPaymentTypes: [],
         selectedFacilities: [],
         selectedJamMalam: null,
         selectedTamuLawanJenis: null,
         selectedTamuMenginap: "Semua",
         selectedBolehHewan: "Semua",
       };
       ```

    2. **Update page.tsx:**
       - Replace the inline `FilterState` type and `DEFAULT_FILTER_STATE` with imports from `./components/filter-types`:
         ```typescript
         import type { FilterState } from "./components/filter-types";
         import { DEFAULT_FILTER_STATE } from "./components/filter-types";
         ```
       - Add FilterPanel to the import line from `./components`:
         ```typescript
         import { Sidebar, PreviewList, LoadingState, ErrorState, EmptyState, FilterPanel } from "./components";
         ```
       - Add the FilterPanel component in the Sidebar children, BEFORE the PreviewList (filters come before preview list in the sidebar). The Sidebar children block becomes:
         ```tsx
         <Sidebar isOpen={sidebarOpen} onToggle={() => setSidebarOpen((v) => !v)}>
           <FilterPanel
             filterState={filterState}
             setFilterState={setFilterState}
             campusList={campusList}
           />
           {loading && <LoadingState />}
           {error && <ErrorState message={error} />}
           {!loading && !error && items.length === 0 && <EmptyState />}
           {!loading && items.length > 0 && (
             <PreviewList
               items={items}
               expanded={previewExpanded}
               onToggleExpand={() => setPreviewExpanded((v) => !v)}
               onItemClick={(kos) => mapViewRef.current?.flyTo(kos)}
             />
           )}
         </Sidebar>
         ```

    3. **Simplify FilterPanel.tsx type import:**
       Update FilterPanel.tsx to import FilterState from `./filter-types` instead of duplicating the type inline:
       ```typescript
       import type { FilterState } from "./filter-types";
       ```
       Remove the inline `type FilterState = { ... }` block from FilterPanel.tsx.

    4. **Also update index.ts to export filter-types:**
       ```typescript
       export * from "./filter-types";
       ```
  </action>
  <verify>
    <automated>
      test -f "frontend/app/prototype/clean-map/components/filter-types.ts" && echo "FILTER_TYPES: PASS" || echo "FILTER_TYPES: FAIL"
      grep -q "FilterPanel" frontend/app/prototype/clean-map/page.tsx && echo "FILTERPANEL_IMPORTED: PASS" || echo "FILTERPANEL_IMPORTED: FAIL"
      grep -q "<FilterPanel" frontend/app/prototype/clean-map/page.tsx && echo "FILTERPANEL_USED: PASS" || echo "FILTERPANEL_USED: FAIL"
      ! grep -q "type FilterState" frontend/app/prototype/clean-map/page.tsx && echo "NO_INLINE_TYPE: PASS" || echo "NO_INLINE_TYPE: FAIL (should import from filter-types)"
      grep -q "filter-types" frontend/app/prototype/clean-map/components/index.ts && echo "INDEX_FILTER_TYPES: PASS" || echo "INDEX_FILTER_TYPES: FAIL"
      ! grep -q "StatsBar" frontend/app/prototype/clean-map/page.tsx && echo "NO_STATSBAR: PASS" || echo "NO_STATSBAR: FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/prototype/clean-map/components/filter-types.ts` exists exporting FilterState type and DEFAULT_FILTER_STATE
    - `page.tsx` imports FilterState and DEFAULT_FILTER_STATE from `./components/filter-types`
    - `page.tsx` imports FilterPanel from `./components`
    - `page.tsx` renders `<FilterPanel>` inside `<Sidebar>` as the first child
    - `page.tsx` passes `filterState`, `setFilterState`, and `campusList` as props to FilterPanel
    - `FilterPanel.tsx` imports FilterState from `./filter-types` (no inline type duplication)
    - `index.ts` exports `filter-types`
    - TypeScript build passes (`cd frontend && npx next build` or `npx tsc --noEmit`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser ↔ | No new trust boundaries — all filter controls are client-side visual mocks. No API calls originate from filter components (campus data already fetched at page level). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | S (spoofing) | ChipGroup input | accept | Visual-only UI mock — chip state changes don't affect data. No spoofing surface. |
| T-02-04 | T (tampering) | SearchBar Input | accept | Client-side only, no persistence. Input value stored in React state only. |
</threat_model>

<verification>
1. FilterPanel renders 2 complete accordion sections (Kamar + Pembayaran) with correct controls
2. SearchBar input with teal focus ring, correct placeholder
3. LocationSelector opens dropdown, filters list on type
4. ChipGroup radio mode: clicking one chip deselects others
5. ChipGroup multi mode: clicking chips toggles selection without deselecting others
6. Price inputs accept numeric typing
7. Period select shows dropdown, selection updates display
8. Page renders without StatsBar, with FilterPanel in sidebar
9. TypeScript build passes with zero errors
10. All copy in Bahasa Indonesia matches UI-SPEC §Copywriting Contract
</verification>

<success_criteria>
- FilterPanel component injects as Sidebar child with lifted filter state
- SearchBar and LocationSelector render as standalone bar above accordion (per D-03)
- Kamar accordion renders gender chip group (radio) + AC chip group (radio) (FILT-01, FILT-03)
- Pembayaran accordion renders price inputs + period select + payment chips (multi) (FILT-02, FILT-06)
- Text search input visible and interactive (FILT-07)
- Campus building combobox visible and searchable (FILT-08)
- Accordion sections expand/collapse on click (UX-03)
- All filter controls respond visually to interaction per UI-SPEC §Interaction Contract
- No data mutation occurs — controls update React state only
</success_criteria>

<output>
After completion, create `.planning/phases/02-filter-panel-ui/02-02-SUMMARY.md`
</output>
