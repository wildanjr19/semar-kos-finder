---
phase: 01-prototype-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/types/kos.ts
  - frontend/lib/kos-helpers.ts
autonomous: true
requirements:
  - UX-06
user_setup: []
must_haves:
  truths:
    - "KosClean TypeScript types exist in a single shared file"
    - "Helper functions exist in a single shared file with zero frontend framework dependencies"
    - "Both CleanMapPrototype.tsx and Map.tsx can import from shared files"
  artifacts:
    - path: "frontend/types/kos.ts"
      provides: "All KosClean-related TypeScript types"
      exports:
        - "Destination"
        - "RawDestination"
        - "RouteApiResponse"
        - "HargaItem"
        - "FasilitasCleaned"
        - "PeraturanCleaned"
        - "KontakItem"
        - "KosClean"
        - "RawKos"
        - "CleanKos"
    - path: "frontend/lib/kos-helpers.ts"
      provides: "Pure helper functions for kos data processing"
      exports:
        - "isRecord"
        - "toNumber"
        - "asStringArray"
        - "asHargaItems"
        - "normalizeTamuLawanJenis"
        - "decodeEncodedPolyline"
        - "formatDistanceMeters"
        - "formatDuration"
        - "normalizeJenisKos"
        - "formatPrice"
        - "normalizeCleanKos"
        - "markerColors"
        - "createLabel"
        - "createChip"
        - "appendChipGroup"
  key_links:
    - from: "frontend/types/kos.ts"
      to: "backend/app/models.py"
      via: "Type alignment"
      note: "Types must match Pydantic model fields"
---

<objective>
Extract shared TypeScript types and pure helper functions from the duplicated code in CleanMapPrototype.tsx and Map.tsx into reusable modules.

Purpose: Eliminate duplication between prototype and production map components, establish a single source of truth for data shapes and kos data processing logic.
Output: `frontend/types/kos.ts` and `frontend/lib/kos-helpers.ts`
</objective>

<execution_context>
@/home/evan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/evan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-prototype-foundation/01-CONTEXT.md
@.planning/phases/01-prototype-foundation/01-UI-SPEC.md

## Key interface context

From `frontend/components/CleanMapPrototype.tsx` (lines 6-239):
- Types: Destination, RawDestination, RouteApiResponse, HargaItem, FasilitasCleaned, PeraturanCleaned, KontakItem, KosClean, RawKos, CleanKos
- Helpers: isRecord, toNumber, asStringArray, asHargaItems, normalizeTamuLawanJenis, decodeEncodedPolyline, formatDistanceMeters, formatDuration, normalizeJenisKos, formatPrice, normalizeCleanKos, markerColors, createLabel, createChip, appendChipGroup

From `frontend/components/Map.tsx` (lines 6-239):
- Same types duplicated
- Same helpers duplicated (plus Map-specific ones: normalizeWaHref, normalizeWaPhone, parseKontakString — keep in Map.tsx, do not extract)

From `backend/app/models.py`:
- Pydantic KosClean model is the backend contract; frontend types must align
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract KosClean TypeScript types</name>
  <files>frontend/types/kos.ts</files>
  <read_first>
    - frontend/components/CleanMapPrototype.tsx (types section, lines 6-80)
    - frontend/components/Map.tsx (types section, lines 6-80)
    - backend/app/models.py (KosClean Pydantic model)
  </read_first>
  <action>
Create `frontend/types/kos.ts` with all KosClean-related types extracted from CleanMapPrototype.tsx. Export every type. Types must be pure data shapes — no React, no maplibre imports. Order: Destination → RawDestination → RouteApiResponse → HargaItem → FasilitasCleaned → PeraturanCleaned → KontakItem → KosClean → RawKos → CleanKos. Add a file header comment: `// Shared kos data types — aligned with backend/app/models.py KosClean model`. Verify type field names match the Pydantic model exactly (e.g., `jenis_kos`, `tipe_pembayaran`, `ac_status`).
  </action>
  <verify>
    <automated>test -f frontend/types/kos.ts && grep -c "export type" frontend/types/kos.ts</automated>
    <expect>10</expect>
  </verify>
  <done>
    - `frontend/types/kos.ts` exists with 10 exported types
    - No React or maplibre imports in the file
    - Type field names match backend Pydantic model
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract shared helper functions</name>
  <files>frontend/lib/kos-helpers.ts</files>
  <read_first>
    - frontend/components/CleanMapPrototype.tsx (helper functions, lines 81-279)
    - frontend/components/Map.tsx (helper functions, lines 81-279)
    - frontend/types/kos.ts (from Task 1)
  </read_first>
  <action>
Create `frontend/lib/kos-helpers.ts` importing types from `../types/kos`. Extract ALL helper functions from CleanMapPrototype.tsx: isRecord, toNumber, asStringArray, asHargaItems, normalizeTamuLawanJenis, decodeEncodedPolyline, formatDistanceMeters, formatDuration, normalizeJenisKos, formatPrice, normalizeCleanKos, markerColors, createLabel, createChip, appendChipGroup. Export each as named export. Functions must remain pure — no React hooks, no DOM manipulation, no maplibre references. `createLabel`, `createChip`, `appendChipGroup` create HTMLElement nodes using standard DOM API (document.createElement) — this is acceptable as pure DOM construction without side effects. Add JSDoc to `normalizeCleanKos` explaining it filters for reviewed status and valid coordinates. Add file header comment: `// Pure helper functions for kos data processing — zero framework dependencies`.
  </action>
  <verify>
    <automated>test -f frontend/lib/kos-helpers.ts && grep -c "export function" frontend/lib/kos-helpers.ts</automated>
    <expect>13</expect>
  </verify>
  <done>
    - `frontend/lib/kos-helpers.ts` exists with 13 exported functions
    - Zero React, Next.js, or maplibre imports
    - Imports types from `../types/kos`
    - `normalizeCleanKos` has JSDoc
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Existing `/api/kos` proxy; no change in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | types/kos.ts | accept | Types are public schema definitions; no secrets |
| T-01-02 | Tampering | kos-helpers.ts normalizeCleanKos | accept | Data validation is client-side convenience; backend is source of truth |
</threat_model>

<verification>
- `grep -c "export type" frontend/types/kos.ts` returns 10
- `grep -c "export function" frontend/lib/kos-helpers.ts` returns 13
- `grep -E "import.*react|import.*maplibre" frontend/types/kos.ts frontend/lib/kos-helpers.ts` returns 0 lines
- `grep "normalizeCleanKos" frontend/lib/kos-helpers.ts | grep -c "@param\|@returns"` returns at least 1
</verification>

<success_criteria>
- Shared types file exists with all 10 types exported
- Shared helpers file exists with all 13 functions exported
- Neither file imports React, Next.js, or maplibre
- Types align with backend Pydantic model field names
</success_criteria>

<output>
After completion, create `.planning/phases/01-prototype-foundation/01-01-SUMMARY.md`
</output>
