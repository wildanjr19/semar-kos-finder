---
phase: 01-prototype-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - frontend/app/prototype/clean-map/components/MapView.tsx
  - frontend/app/prototype/clean-map/components/MapView.module.css
  - frontend/app/prototype/clean-map/page.tsx
  - frontend/components/CleanMapPrototype.tsx
autonomous: true
requirements:
  - UX-06
user_setup: []
must_haves:
  truths:
    - "MapView encapsulates maplibre initialization, markers, popups, and route drawing"
    - "Page.tsx composes MapView + Sidebar with lifted state"
    - "Data fetching lives at page level, passed down via props"
    - "CleanMapPrototype.tsx monolith is removed or simplified to re-export"
    - "User can navigate to /prototype/clean-map and see working map with markers"
  artifacts:
    - path: "frontend/app/prototype/clean-map/components/MapView.tsx"
      provides: "MapLibre map container with markers and route layer"
      min_lines: 120
    - path: "frontend/app/prototype/clean-map/page.tsx"
      provides: "Page composition with lifted state"
      min_lines: 60
    - path: "frontend/components/CleanMapPrototype.tsx"
      provides: "Backwards compatibility re-export or removed"
  key_links:
    - from: "page.tsx"
      to: "/api/kos"
      via: "fetch in useEffect"
    - from: "page.tsx"
      to: "MapView.tsx"
      via: "props: items, destinations, onMarkerClick"
    - from: "page.tsx"
      to: "Sidebar.tsx"
      via: "props: isOpen, onToggle, children"
    - from: "MapView.tsx"
      to: "Popup.tsx"
      via: "maplibregl.Popup.setDOMContent with Popup React element"
---

<objective>
Extract the MapView container from CleanMapPrototype.tsx monolith, wire the composed page with lifted state, and retire the monolith.

Purpose: Complete the prototype decomposition by separating map imperatives from React UI; establish the page-level state pattern for Phase 2 filter injection.
Output: `MapView.tsx`, updated `page.tsx`, and simplified/removed `CleanMapPrototype.tsx`
</objective>

<execution_context>
@/home/evan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/evan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-prototype-foundation/01-CONTEXT.md
@.planning/phases/01-prototype-foundation/01-UI-SPEC.md
@frontend/types/kos.ts
@frontend/lib/kos-helpers.ts
@frontend/app/prototype/clean-map/components/Popup.tsx
@frontend/app/prototype/clean-map/components/Sidebar.tsx
@frontend/app/prototype/clean-map/components/StatsBar.tsx
@frontend/app/prototype/clean-map/components/PreviewList.tsx
@frontend/app/prototype/clean-map/components/LoadingState.tsx
@frontend/app/prototype/clean-map/components/ErrorState.tsx
@frontend/app/prototype/clean-map/components/RouteControls.tsx

## Existing code patterns

From `frontend/components/CleanMapPrototype.tsx`:
- Map init: lines 343-377 (useEffect with maplibregl.Map, osm raster tiles, NavigationControl)
- Marker rendering: lines 437-747 (useEffect dependent on [items, destinations, mapReady])
- Route drawing: lines 294-341 (clearRoute, drawRoute functions)
- Data fetching: lines 379-435 (loadKos, loadDestinations useEffects)
- Sidebar: lines 760-885 (inline JSX with stats, preview, loading, error)

From `frontend/app/prototype/clean-map/page.tsx`:
- Currently just dynamically imports CleanMapPrototype with ssr: false

## Key decisions from CONTEXT.md

- D-08: Use component props + lifting state approach
- D-09 (agent discretion): Create lifted state structure with empty/default filter values now — prepares for Phase 2
- D-10 (agent discretion): Lift data fetching to page level in Phase 1
- D-11 (agent discretion): Lift sidebar open/closed state to page level
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MapView container component</name>
  <files>
    frontend/app/prototype/clean-map/components/MapView.tsx
    frontend/app/prototype/clean-map/components/MapView.module.css
  </files>
  <read_first>
    - frontend/components/CleanMapPrototype.tsx (map init, markers, route logic)
    - frontend/types/kos.ts
    - frontend/lib/kos-helpers.ts
    - frontend/app/prototype/clean-map/components/Popup.tsx
  </read_first>
  <action>
Create MapView.tsx + MapView.module.css. Props: `{ items: CleanKos[]; destinations: Destination[]; onMarkerClick?: (kos: CleanKos) => void }`.

Encapsulate ALL maplibre imperative logic from CleanMapPrototype.tsx:
1. **Map initialization** (useEffect, [] deps): Create maplibregl.Map with osm raster tiles, center [110.856, -7.559], zoom 14. Add NavigationControl. Set mapReady state. Cleanup: remove markers, remove map.
2. **Marker rendering** (useEffect, [items, destinations, mapReady] deps): Clear existing markers. For each CleanKos, create marker element (38px circle, gender colors from `markerColors`), attach Popup using `maplibregl.Popup.setDOMContent`. The popup DOM content is rendered by mounting a React root into a div, then passing that div to setDOMContent. This pattern: create a div, ReactDOM.createRoot(div).render(<Popup kos={kos} ... />), then popup.setDOMContent(div). When popup closes, unmount the root. Alternatively, use a simpler pattern: since Popup.tsx is now a React component, create a container div and render Popup into it via a portal or createRoot.
3. **fitBounds** (same useEffect as markers): If items.length > 0 and not yet fitted, calculate bounds and fit with padding. Mobile padding 64; desktop padding `{ top: 88, right: 64, bottom: 64, left: 380 }`. Use window.innerWidth < 760 check.
4. **Route layer** (useImperativeHandle or callback props): Expose `flyTo(kos: CleanKos)` and `clearRoute()` methods via ref using `useImperativeHandle`. The route drawing logic (drawRoute, clearRoute) stays inside MapView; Popup/RouteControls trigger it through callbacks passed down from page level.

CSS module: Map container is `width: 100%; height: 100%`. No other styles needed.

Import maplibregl dynamically or ensure this component is only rendered client-side (the parent page.tsx already handles ssr: false for the dynamic import, but MapView itself doesn't need dynamic import since page.tsx will import it normally within the dynamic boundary).
  </action>
  <verify>
    <automated>test -f frontend/app/prototype/clean-map/components/MapView.tsx && grep -c "maplibregl.Map" frontend/app/prototype/clean-map/components/MapView.tsx</automated>
    <expect>1</expect>
  </verify>
  <done>
    - MapView.tsx exists with map init, markers, route layer
    - Uses `useImperativeHandle` to expose flyTo and clearRoute
    - Popup integration renders Popup React component into maplibregl popup
    - fitBounds handles mobile/desktop padding
    - MapView.module.css exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire page composition and lift state</name>
  <files>
    frontend/app/prototype/clean-map/page.tsx
    frontend/components/CleanMapPrototype.tsx
  </files>
  <read_first>
    - frontend/app/prototype/clean-map/page.tsx (current)
    - frontend/components/CleanMapPrototype.tsx (full)
    - frontend/app/prototype/clean-map/components/MapView.tsx
    - frontend/app/prototype/clean-map/components/Sidebar.tsx
    - frontend/app/prototype/clean-map/components/StatsBar.tsx
    - frontend/app/prototype/clean-map/components/PreviewList.tsx
    - frontend/app/prototype/clean-map/components/LoadingState.tsx
    - frontend/app/prototype/clean-map/components/ErrorState.tsx
    - frontend/types/kos.ts
  </read_first>
  <action>
Update `frontend/app/prototype/clean-map/page.tsx` to compose the decomposed components with lifted state.

**State** (useState at page level):
- `items: CleanKos[]` — loaded kos data
- `destinations: Destination[]` — loaded master-uns destinations
- `loading: boolean` — data loading state
- `error: string` — error message
- `sidebarOpen: boolean` — sidebar visibility, default `true`
- `previewExpanded: boolean` — preview list expanded, default `false`

**Data fetching** (useEffect, [] deps):
- Fetch `/api/kos` with cache: "no-store". Normalize with `normalizeCleanKos`. Set items.
- Fetch `/api/master-uns` with cache: "no-store". Normalize destinations. Set destinations.
- Both share loading/error state.

**Refs**:
- `mapViewRef = useRef<MapViewHandle>(null)` — for calling flyTo when preview item clicked

**Render structure**:
```tsx
<div className={styles.page}> {/* position: relative, height: 100dvh, width: 100%, overflow: hidden */}
  <MapView
    ref={mapViewRef}
    items={items}
    destinations={destinations}
  />
  <Sidebar isOpen={sidebarOpen} onToggle={() => setSidebarOpen((v) => !v)}>
    {/* Header is inside Sidebar */}
    <StatsBar items={items} />
    {/* Filter chips placeholder — empty div for Phase 2 */}
    {loading && <LoadingState />}
    {error && <ErrorState message={error} />}
    {!loading && !error && items.length === 0 && (
      <div>Belum ada kos reviewed. Approve data dari admin dulu.</div>
    )}
    {!loading && items.length > 0 && (
      <PreviewList
        items={items}
        expanded={previewExpanded}
        onToggleExpand={() => setPreviewExpanded((v) => !v)}
        onItemClick={(kos) => mapViewRef.current?.flyTo(kos)}
      />
    )}
  </Sidebar>
</div>
```

Create `page.module.css` for the page container (position, height, overflow).

**CleanMapPrototype.tsx**: Since the monolith is now decomposed, replace `frontend/components/CleanMapPrototype.tsx` with a simple re-export that imports the page composition, OR delete it and update any imports. Check if any other file imports CleanMapPrototype. If only page.tsx imports it, delete the file. If other files import it, keep a thin re-export wrapper:
```tsx
// Re-export for backwards compatibility during transition
export { default } from "../app/prototype/clean-map/page";
```
But this creates a circular import risk. Better: just delete it and update page.tsx to not import it.
  </action>
  <verify>
    <automated>test -f frontend/app/prototype/clean-map/page.tsx && grep -c "MapView\|Sidebar\|StatsBar\|PreviewList\|LoadingState\|ErrorState" frontend/app/prototype/clean-map/page.tsx</automated>
    <expect>6</expect>
  </verify>
  <done>
    - page.tsx composes MapView + Sidebar with 6+ sub-components imported
    - State lifted to page level (items, destinations, loading, error, sidebarOpen, previewExpanded)
    - Data fetching lives in page.tsx useEffect
    - PreviewList flyTo calls mapViewRef.current.flyTo
    - CleanMapPrototype.tsx is deleted or simplified
    - page.module.css exists for container layout
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | `/api/kos` and `/api/master-uns` fetches from page.tsx; existing endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | page.tsx fetch error messages | accept | Error messages come from fetch responses; no sensitive data |
| T-01-03-02 | Denial of Service | page.tsx dual fetch on mount | accept | Existing behavior; no additional requests added |
</threat_model>

<verification>
- `grep -c "MapView\|Sidebar\|StatsBar\|PreviewList\|LoadingState\|ErrorState" frontend/app/prototype/clean-map/page.tsx` returns ≥ 6
- `grep -c "useState" frontend/app/prototype/clean-map/page.tsx` returns ≥ 3
- `grep -c "useEffect" frontend/app/prototype/clean-map/page.tsx` returns ≥ 1
- `grep -c "normalizeCleanKos" frontend/app/prototype/clean-map/page.tsx` returns 1
- `grep -c "maplibregl" frontend/app/prototype/clean-map/page.tsx` returns 0 (maplibre stays in MapView)
- `test -f frontend/app/prototype/clean-map/page.module.css`
</verification>

<success_criteria>
- MapView.tsx encapsulates all maplibre logic (init, markers, route, fitBounds)
- page.tsx lifts state and composes all sub-components
- Data fetching lives at page level, not inside MapView
- CleanMapPrototype.tsx monolith is removed or reduced to re-export
- /prototype/clean-map renders working map with markers, sidebar, stats, preview
- No maplibre imports in page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/01-prototype-foundation/01-03-SUMMARY.md`
</output>
