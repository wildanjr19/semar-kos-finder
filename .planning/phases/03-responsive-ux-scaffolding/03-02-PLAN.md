---
phase: 03-responsive-ux-scaffolding
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/app/prototype/clean-map/components/Sidebar.tsx
  - frontend/app/prototype/clean-map/components/Sidebar.module.css
  - frontend/app/prototype/clean-map/page.tsx
autonomous: false
requirements: [UX-04, UX-05]
must_haves:
  truths:
    - "D-04: On mobile, filter panel collapses to a handle/count summary and opens over bottom 70dvh while map remains visible."
    - "D-05: On desktop, existing collapsible left sidebar behavior remains intact."
    - "D-06: On mobile, preview list remains below filter controls inside the bottom sheet."
  artifacts:
    - path: "frontend/app/prototype/clean-map/components/Sidebar.tsx"
      provides: "Responsive sidebar/bottom-sheet shell and mobile summary affordance"
      exports: ["Sidebar"]
    - path: "frontend/app/prototype/clean-map/components/Sidebar.module.css"
      provides: "Desktop sidebar styles plus mobile bottom sheet styles"
      contains: "70dvh"
    - path: "frontend/app/prototype/clean-map/page.tsx"
      provides: "Result count passed to Sidebar mobile summary"
  key_links:
    - from: "frontend/app/prototype/clean-map/page.tsx"
      to: "frontend/app/prototype/clean-map/components/Sidebar.tsx"
      via: "passes resultCount={items.length}"
      pattern: "resultCount=\{items\.length\}"
    - from: "frontend/app/prototype/clean-map/components/Sidebar.tsx"
      to: "frontend/app/prototype/clean-map/components/Sidebar.module.css"
      via: "uses mobile sheet classes"
      pattern: "mobileHandle|sheetHandle|mobileSummary"
    - from: "frontend/app/prototype/clean-map/components/Sidebar.tsx"
      to: "children"
      via: "renders children inside sheet content so FilterPanel and PreviewList stay together"
      pattern: "\{children\}"
---

<objective>
Make the existing prototype shell responsive without changing filtering scope.

Purpose: Users on desktop keep the current collapsible left sidebar; users on mobile get a bottom-sheet filter/preview scaffold that keeps the map visible.
Output: Responsive Sidebar API, mobile handle/count summary, CSS-only desktop/mobile layout behavior, and page wiring.
</objective>

<execution_context>
@/home/evan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/evan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-responsive-ux-scaffolding/03-CONTEXT.md
@.planning/phases/03-responsive-ux-scaffolding/03-01-PLAN.md
@.planning/phases/02-filter-panel-ui/02-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@frontend/app/prototype/clean-map/page.tsx
@frontend/app/prototype/clean-map/components/Sidebar.tsx
@frontend/app/prototype/clean-map/components/Sidebar.module.css
@frontend/app/prototype/clean-map/components/PreviewList.tsx

<interfaces>
Existing Sidebar contract to extend, not replace:
```ts
type SidebarProps = {
  isOpen: boolean;
  onToggle: () => void;
  children: ReactNode;
};

export function Sidebar({ isOpen, onToggle, children }: SidebarProps)
```

Required new prop:
```ts
resultCount: number;
```

Existing page composition that must preserve PreviewList as Sidebar child:
```tsx
<Sidebar isOpen={sidebarOpen} onToggle={() => setSidebarOpen((v) => !v)}>
  <FilterPanel ... />
  {loading && <LoadingState />}
  {error && <ErrorState message={error} />}
  {!loading && !error && items.length === 0 && <EmptyState />}
  {!loading && items.length > 0 && <PreviewList ... />}
</Sidebar>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Sidebar with responsive bottom-sheet semantics</name>
  <files>frontend/app/prototype/clean-map/components/Sidebar.tsx, frontend/app/prototype/clean-map/page.tsx</files>
  <read_first>
    - frontend/app/prototype/clean-map/components/Sidebar.tsx
    - frontend/app/prototype/clean-map/page.tsx
    - .planning/phases/03-responsive-ux-scaffolding/03-CONTEXT.md
  </read_first>
  <action>
    Update `SidebarProps` with `resultCount: number`. In `Sidebar`, render a mobile-only toggle/summary button before the scrollable content with a visual drag handle, text `Filter & preview`, and count `{resultCount} kos ditemukan` per D-04. Keep existing desktop header, `Map utama` link, and FAB behavior. The mobile summary button must call `onToggle`, set `aria-expanded={isOpen}`, and have `aria-label` values `Buka filter dan preview` / `Tutup filter dan preview`. Wrap the existing header+children in a content div if needed so mobile collapsed state can hide content while leaving the handle visible. In `page.tsx`, pass `resultCount={items.length}` to Sidebar. Do not move PreviewList out of Sidebar; D-06 requires it remains below filter controls inside bottom sheet.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Sidebar.tsx` contains `resultCount: number`.
    - `Sidebar.tsx` contains the exact text `Filter & preview`.
    - `Sidebar.tsx` contains the exact text `kos ditemukan`.
    - `Sidebar.tsx` contains `aria-expanded={isOpen}`.
    - `page.tsx` contains `resultCount={items.length}`.
    - `page.tsx` still renders `<PreviewList` between `<Sidebar` and `</Sidebar>`.
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Sidebar API supports mobile count summary while preserving existing desktop children composition.</done>
</task>

<task type="auto">
  <name>Task 2: Implement desktop sidebar preservation and mobile bottom-sheet CSS</name>
  <files>frontend/app/prototype/clean-map/components/Sidebar.module.css</files>
  <read_first>
    - frontend/app/prototype/clean-map/components/Sidebar.module.css
    - frontend/app/prototype/clean-map/components/Sidebar.tsx
    - .planning/phases/03-responsive-ux-scaffolding/03-CONTEXT.md
  </read_first>
  <action>
    Modify `Sidebar.module.css` so desktop (`min-width: 760px` behavior via default styles) preserves the current absolute left sidebar, width `min(356px, calc(100vw - 32px))`, `max-height: calc(100dvh - 32px)`, and translateX hidden behavior per D-05. Add mobile rules under `@media (max-width: 759px)` that convert `.sidebar` into a bottom sheet: `top: auto`, `left: 0`, `right: 0`, `bottom: 0`, `width: 100%`, `max-height: 70dvh`, rounded top corners, no bottom radius, and `transform: translateY(calc(100% - 72px))` when hidden/collapsed so only handle/count summary remains visible per D-04. Add classes for `.mobileHandle`, `.handlePill`, `.mobileSummary`, and `.content` as used by Sidebar.tsx. Ensure mobile `.fab` is hidden because the bottom-sheet handle becomes the toggle. Keep transitions at 150–300ms and use transform/opacity only. Preserve overflow-y auto inside content so FilterPanel and PreviewList scroll within the 70dvh sheet.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Sidebar.module.css` contains `max-height: 70dvh`.
    - `Sidebar.module.css` contains `translateY(calc(100% - 72px))`.
    - `Sidebar.module.css` contains `.mobileHandle`.
    - `Sidebar.module.css` contains `.handlePill`.
    - `Sidebar.module.css` contains `.mobileSummary`.
    - `Sidebar.module.css` contains `.content`.
    - `Sidebar.module.css` contains `display: none` inside mobile `.fab` rule.
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Desktop sidebar remains functionally unchanged; mobile bottom sheet opens to 70dvh and collapses to handle/count summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify responsive UX behavior</name>
  <what-built>Desktop left sidebar preservation and mobile bottom-sheet scaffolding with count summary and preview list retained.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`.
    2. Visit `http://localhost:3000/prototype/clean-map` at desktop width >= 760px. Expected: left sidebar appears, existing round FAB toggles collapse/expand, map remains behind it.
    3. In browser responsive mode set width 390px. Expected: bottom sheet is visible at bottom as a handle/count summary when collapsed, opens to about 70dvh when tapped, and map remains visible above the sheet.
    4. In mobile open state, select filters and expand preview. Expected: active chips/count appear, accordion controls remain usable, and PreviewList stays below the filter panel in the sheet.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap/click → client UI state | Sidebar/bottom-sheet open state changes based on untrusted browser interaction. |
| Loaded item count → DOM | Count derived from `/api/kos` response length and rendered as text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | D | Mobile bottom sheet | mitigate | Use CSS transform transitions only, keep sheet max-height at 70dvh, keep content scroll contained to prevent layout thrash. |
| T-03-02-02 | I | Result count display | accept | Count is aggregate listing count already visible through public markers/list; no new sensitive data exposed. |
| T-03-02-03 | E | Sidebar toggle controls | accept | Toggle controls only affect local UI state; no privilege or route change. |
</threat_model>

<verification>
Automated: `cd frontend && npx tsc --noEmit`. Manual checkpoint verifies desktop and mobile responsive behavior because viewport-specific visual layout cannot be fully proven by existing automated tests.
</verification>

<success_criteria>
- UX-04 covered: mobile bottom sheet opens over bottom 70dvh and keeps map visible.
- UX-05 covered: desktop sidebar remains collapsible left sidebar.
- D-04, D-05, and D-06 implemented exactly.
- PreviewList remains inside Sidebar after FilterPanel on mobile.
- No real filtering, backend query params, URL sync, or desktop width redesign added.
</success_criteria>

<output>
After completion, create `.planning/phases/03-responsive-ux-scaffolding/03-02-SUMMARY.md`.
</output>
